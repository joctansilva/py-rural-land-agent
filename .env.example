# Banco de dados
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=fazendas
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123

# LLM — preencha apenas UMA das chaves abaixo.
# A prioridade de seleção é: OpenAI → Anthropic → Google Gemini.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY= 
# GOOGLE_API_KEY=

# Modelo (opcional — se vazio, usa o padrão do provider selecionado)

# OpenAI padrão:  gpt-4o-mini
# Anthropic padrão: claude-haiku-4-5-20251001
# Google padrão: gemini-2.0-flash
AGENT_MODEL=

# Configurações do agente
AGENT_MAX_TOKENS=1024
DEBUG=false
